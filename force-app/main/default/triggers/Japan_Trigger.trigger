trigger Japan_Trigger on Account (before insert) {
  /*  for(Account a: trigger.new){
        String accname=a.Name;
        if(accname.isWhitespace()){
            List<String> arr=accname.split(' ');
            for(String s: arr){
                
            }
        }
    }
*/
}